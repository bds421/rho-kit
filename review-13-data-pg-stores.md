# Code review: Postgres data stores (stage 1 — unverified findings)

## Scope

- **Directories**: data/actionlog/postgres, data/apikey/postgres, data/approval/postgres, data/cron/pgstore, data/idempotency/pgstore, data/saga/pgstore, data/lock/pgadvisory
- **Git ref**: main @ 9c370ea2 (v2.3.1 prep)
- **Review lens results**: 12 (lenses inferred: correctness, design, security; expected lens count: 3)
- Status: raw reviewer findings; adversarial verification (stage 2) pending.

## Summary

| Severity | Count |
|---|---|
| CRITICAL | 0 |
| HIGH | 0 |
| MEDIUM | 0 |
| LOW | 0 |
| **Total (deduplicated)** | **0** |

**Reviewer impressions:**

> This is unusually disciplined data-layer code: every user-supplied value is bound via placeholders, dynamic table names are regex-validated identifiers, errors are routed through redact.WrapError so driver text never leaks, lock/transaction semantics (advisory locks for first-append races, SELECT FOR UPDATE, owner-token-guarded idempotency writes, discard-on-unconfirmed-unlock in pgadvisory) are correct and extensively documented with rationale. The remaining issues are second-order: tenant scoping is rigorously enforced on List paths but absent on id-keyed reads/mutations (most consequential for approval decisions), a legacy NULL-fingerprint replay window in the idempotency store, and a few consistency gaps in migration hygiene and input bounding.

> This is unusually careful data-layer code: every user value is bound via placeholders (dynamic identifiers are regexp-gated), locking semantics (advisory locks, SELECT FOR UPDATE, ON CONFLICT lock stealing) are correct and exhaustively documented with rationale, tenant scoping is enforced by Query.Validate in the packages that need it, and integration tests exist for every backend under testing/integrationtest. The findings are mostly consistency and polish issues — sibling packages diverging on error typing and nil-receiver conventions, a missing pagination index in approval, and one genuine round-trip precision bug in approval's Decide that contradicts the package's own documented guarantee.

> This scope is unusually high quality: consistently parameterized SQL (identifier interpolation is regex-gated), correct keyset pagination with signed cursors, careful lock hygiene (advisory-lock discard-on-uncertain-unlock in pgadvisory is exemplary), tenant scoping enforced via Query.Validate, and rich comments documenting prior audit fixes. The surviving issues are edge cases and one design-level gap: the saga store's advertised multi-replica safety is not actually delivered by its write path, and two stores have subtle precision/serialization mismatches (actionlog microsecond truncation, idempotency headers escaping) that can permanently break verification or replay for legal inputs.

> This is unusually disciplined data-layer code: every user/operator value is bound via placeholders, dynamic SQL is limited to regex-validated table identifiers, cursors are HMAC-SHA256 signed with constant-time verification and length caps, lock tokens use crypto/rand, driver errors are consistently redact-wrapped, and locking semantics (advisory locks, SELECT FOR UPDATE, ON CONFLICT owner-token gating, bad-conn discarding in pgadvisory) are carefully reasoned with audit references in comments. The findings that remain are edge cases and design gaps rather than exploitable injections: the most notable is that approval (and actionlog) point reads/mutations are id-only while the same packages enforce strict tenant scoping on their list paths, pushing multi-tenant isolation onto every caller.

> This is unusually disciplined data-layer code: all values are parameterized, dynamic identifiers (table names) are regex-gated at construction, locking semantics (advisory xact lock + SELECT FOR UPDATE in actionlog, row locks in approval, owner-token CAS in idempotency, discard-on-uncertain-unlock in pgadvisory) are carefully reasoned and documented, and tenant scoping is enforced at the query-contract level (ErrQueryTenantRequired/AllTenants). The findings are mostly consistency and hardening gaps between siblings — saga/pgstore missing the embedded Migrations FS and the IF-NOT-EXISTS convention, actionlog missing the timestamp-precision normalization approval already has, cron's untyped errors — plus a few hot-path/unbounded-query performance nits; nothing rose to an exploitable or data-corrupting defect within the sanctioned call paths.

> This scope is unusually high quality: every query is parameterised (dynamic identifiers are regexp-validated or constant), transactions consistently pair SELECT FOR UPDATE / advisory locks with correct rollback-on-defer, rows are always closed and rows.Err() checked, and hard-won races (advisory-lock leak on cancelled unlock, first-append serialisation, empty-body cache discrimination) are handled and documented with rationale. The surviving findings are edge-case classification and parity gaps — the most notable being actionlog treating any unique violation (including a duplicate primary key) as the documented-retryable ErrSeqCollision — plus a couple of non-atomic two-statement patterns and small cross-backend inconsistencies.

> This family of Postgres stores is unusually well-defended and high quality: every dynamic identifier is regexp-validated and only ever interpolated as a table name (all values go through positional placeholders, so there is no SQL-injection surface), tenant scoping on the list paths is enforced via Query.Validate, and the concurrency-sensitive paths (actionlog's per-tenant advisory-lock + SELECT FOR UPDATE, approval/saga's row-lock transactions, idempotency's INSERT…ON CONFLICT lock acquisition, pgadvisory's session-vs-transaction lock lifecycles and bad-connection discarding) are correct and carry thorough rationale comments. Migrations are additive and idempotent. The only issues I found are minor: a sub-millisecond interval-truncation edge case in saga ListResumable and cron's Store missing the nil-receiver guard its siblings all have.

> This is a mature, carefully-written family of Postgres stores: all queries use positional placeholders (no SQL injection surface), table-name interpolation is gated by identifier regexes, tenant scoping and keyset pagination are correct, and the transaction/advisory-lock semantics in actionlog and approval are sound and unusually well-commented. The main weaknesses are API-consistency issues across sibling backends rather than outright bugs: the saga Put's implicit UpdatedAt-driven insert/update routing is not substitutable with the memory backend, and several Create/Add paths bury duplicate-key violations in opaque errors where siblings expose typed sentinels. No CRITICAL or HIGH data-loss/security defects were found within this lens.

> High-quality, defensively-written scope. SQL is uniformly parameterized, table-name interpolation is regex-guarded at construction with unexported struct fields (no external injection path), transaction/locking semantics are carefully reasoned (advisory lock + FOR UPDATE for first-append serialization, connection-discard on unconfirmed advisory unlock, SHA-256 keyspace to defeat adversarial lock-id collisions), and migrations are idempotent with sensible constraints. Errors are consistently redacted and traces deliberately omit user-controlled keys, so I found no injection, authz-bypass, or crypto-misuse defect — only two low-severity asymmetries in defense-in-depth (an ungated fingerprint column and tenant scoping enforced on List but not on id-addressed reads/mutations).

> This family of Postgres stores is well above average for a security review: every value reaches SQL through positional bind parameters, the only string-interpolated SQL fragments are table names (all constrained by a strict identifier regex validated at construction) and placeholder indices, driver errors are consistently wrapped through redact to avoid PII/secret leakage, tracing deliberately omits caller-controlled keys, and tenant-scoped List paths correctly force an explicit TenantID-or-AllTenants decision (audit FR-053) so cross-tenant listing can never happen by omission. The advisory-lock backend also correctly moved off FNV to SHA-256 and hardens the session-lock connection-discard paths. The two findings are a genuine multi-replica concurrency gap in the saga store and a minor cross-tenant lock-contention nit in actionlog; I found no injection, authz-bypass, crypto-misuse, or secret-leak issues in scope.

> This is a high-quality, unusually well-documented set of Postgres stores: every query uses positional parameters (no SQL-injection surface), table-name overrides are regex-validated identifiers, tenant scoping is enforced at the query-validation layer (and via an explicit TenantStore wrapper for approval's unscoped Get), and locking/transaction semantics (advisory locks, SELECT FOR UPDATE, ON CONFLICT lock-steal, session-vs-tx advisory locks with connection-discard on lost round-trips) are handled carefully with thorough rationale comments. The remaining findings are mostly minor consistency, error-taxonomy, and hot-path efficiency nits rather than correctness or security defects; the only one worth prioritizing is the unbounded ListResumable materialization on the saga recovery path.

> This family of Postgres stores is high quality and clearly already hardened by prior audits: every query is parameterized, table identifiers are regex-validated or hardcoded (no SQL injection surface), tenant scoping is enforced upstream at Query.Validate, and the concurrency-sensitive paths (actionlog per-tenant advisory lock + SELECT FOR UPDATE, approval decide/mark-executed under row locks, idempotency INSERT...ON CONFLICT lock acquisition, pgadvisory connection-discard on unconfirmable unlocks) are correct and well-reasoned. Timestamp precision is deliberately truncated to microseconds to keep signed canonical forms round-trippable, and migrations are valid goose-format files matched by the kit's goose-based migrate tool. The only issues I could substantiate are low-severity edge cases around fingerprint handling and a sub-millisecond interval truncation; I found no CRITICAL or HIGH correctness/concurrency defects.

## Findings

_All stage-1 findings for this family are fixed or applied as intentional v2 breaks. See V3_BREAKING_PROPOSALS.md (APPLIED) and git history._
