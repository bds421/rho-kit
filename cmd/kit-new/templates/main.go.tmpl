// Command {{.ServiceName}} is the entry point for the service.
//
// Generated by kit-new. To regenerate, run:
//
//	kit-new {{.ServiceName}} --module-path={{.ModulePath}}
package main

import (
	"log/slog"

	"github.com/bds421/rho-kit/app"

	svcapp "{{.ModulePath}}/internal/app"
)

// version is overwritten at build time via -ldflags. Empty falls back to
// the embedded build info (vcs.revision + vcs.modified) so go run still
// emits a meaningful version field on every log line.
var version = ""

func main() {
	app.Main("{{.ServiceName}}", version, func(logger *slog.Logger) error {
		return svcapp.Run(logger)
	})
}
