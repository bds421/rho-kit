// Command {{.ServiceName}} is the entry point for the service.
//
// Generated by kit-new. To regenerate, run:
//
//	kit-new {{.ServiceName}} --module-path={{.ModulePath}}
package main

import (
	"log/slog"

	"github.com/bds421/rho-kit/app/v2"

	svcapp "{{.ModulePath}}/internal/app"
)

// version is overwritten at build time via -ldflags. When empty,
// health.ResolveVersion falls back to the APP_VERSION environment
// variable (injected by the release pipeline); with neither set (e.g.
// plain `go run`) the version field is reported empty.
var version = ""

func main() {
	app.Main("{{.ServiceName}}", version, func(logger *slog.Logger) error {
		return svcapp.Run(logger, version)
	})
}
