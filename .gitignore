.DS_Store
coverage.out
vendor/

# Built command binaries — every cmd/<name>/<name> artifact.
# Reproducible builds go into dist/ (also ignored).
cmd/kit-bench-gate/kit-bench-gate
cmd/kit-doctor/kit-doctor
cmd/kit-migrate/kit-migrate
cmd/kit-new/kit-new
cmd/kit-verify/kit-verify

# Same binaries when accidentally built from repo root
# (`go build ./cmd/<name>/...` drops the output in $PWD).
/kit-bench-gate
/kit-doctor
/kit-migrate
/kit-new
/kit-verify

# Scratch task / review prompt files used during release prep.
/task.md

# build artifacts
dist/

# Workspace + review scratch state that must never be committed.
go.work.backup
go.work.local
V2_REVIEW_FINDINGS.md
docs/release/rehearsals/*.log

# Claude
.claude/
