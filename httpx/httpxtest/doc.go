package httpxtest

// The package doc comment for httpxtest lives in httpxtest.go. This file
// intentionally carries no doc comment so go/doc renders a single,
// authoritative package overview (avoiding the prior divergent text here
// that referenced an "authenticated contexts" helper the package never
// provided).
