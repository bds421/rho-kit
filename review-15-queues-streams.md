# Code review: Queues & streams (stage 1 — unverified findings)

## Scope

- **Directories**: data/queue/redisqueue, data/queue/riverqueue, data/stream/redisstream
- **Git ref**: main @ 9c370ea2 (v2.3.1 prep)
- **Review lens results**: 12 (lenses inferred: correctness, design, security; expected lens count: 3)
- Status: raw reviewer findings; adversarial verification (stage 2) pending.

## Summary

| Severity | Count |
|---|---|
| CRITICAL | 0 |
| HIGH | 0 |
| MEDIUM | 0 |
| LOW | 0 |
| **Total (deduplicated)** | **0** |

**Reviewer impressions:**

> This scope is well above average for security hygiene: every external input (queue/stream names, message IDs, types, payloads, headers) is validated with explicit byte caps and character allowlists, error strings pass through a redaction layer, metric labels are opaqued, and at-least-once/crash-window semantics are documented with unusual honesty (including prior audit findings referenced inline). The remaining issues are second-order: the dead-letter path undoes the input-size defenses by copying rejected entries verbatim, a couple of config interactions (handlerTimeout vs claimMinIdle, DLQ==source stream) lack the cross-validation the package otherwise applies aggressively, and two fail-open fallbacks (delivery count assumed 1, context-less inspector calls) can silently weaken poison-message termination and health probing. No injection, crypto, secret-leak, or tenant-isolation flaws were found.</overallImpression>
</invoke>

> This is unusually careful, well-documented code: at-least-once semantics, crash windows, dedupe scopes, and poison-message handling are all reasoned about explicitly in godoc and comments, validation is thorough, and past audit fixes (FR-059/062/063, metric double-counting) are encoded as tests. The main weaknesses are cross-sibling inconsistencies — redisqueue's eager default-metrics registration repeats the exact bug redisstream fixed, riverqueue lacks the permanent-error/poison short-circuit both siblings have, and option semantics (enqueue-time vs consume-time knobs, nil-registerer contracts, asynq sentinel leakage) create hold-it-wrong traps for callers moving between backends. Test coverage is strong for validation and options but thin around the trickiest PEL/claim recovery logic.

> This is unusually disciplined library code: exhaustive godoc explaining every non-obvious semantic (asynq lease vs invisibility timeout, XAUTOCLAIM cursor advancement, XGROUP DELCONSUMER data-loss trap), consistent panic-on-misconfiguration, defensive panic recovery around handlers, and careful metric accounting for partial-failure paths. The surviving defects cluster in the failure/edge paths of the dead-letter and retry machinery — the non-atomic XADD+XACK pipeline in redisstream is the one genuine data-loss bug, and the riverqueue adapter is noticeably less mature than its two siblings (no permanent-error mapping). Concurrency primitives (CAS single-use guard, active-queue mutex, claim-loop join-before-cleanup ordering) are all used correctly.

> This scope is well above average quality: input validation is systematic (bounded names, IDs, payloads, header counts/bytes, pre-unmarshal size caps), Redis commands go through parameterized go-redis args so injection surface is nil, metric labels are hashed via OpaqueLabelValue, and the redact package is applied consistently to keep tenant data out of logs and error strings. At-least-once, crash-window, and idempotency caveats are unusually well documented in godoc. Remaining issues are edge-case rather than exploitable: DLQ bounding by count instead of bytes, a fail-open retry counter, an unredacted asynq logger passthrough, and the riverqueue backend missing the permanent-error/poison-message short-circuit its siblings have.

> This is unusually high-quality infrastructure code: at-least-once semantics, poison-message routing, crash windows, and ack ordering are all reasoned about explicitly in comments, invariants are checked in ready() gates, error strings follow a consistent redact discipline, and past audit findings (FR-059/062/063, N+1 delivery-count fetches, XAUTOCLAIM cursor semantics) are documented in place. The residual issues are almost entirely cross-backend consistency and misuse-resistance polish — the three sibling packages disagree on WithMaxRetries(0) semantics, duplicate-ID enqueue behaviour, and permanent-error handling, and the redisstream claim/retry timing knobs can be combined into concurrent duplicate processing without any constructor-level guard. Test coverage is strong for validation and metrics but thin around the trickiest PEL-recovery paths.

> This scope is unusually high quality: at-least-once semantics, ack/dead-letter crash windows, poison-message routing, and metric double-counting hazards are all explicitly reasoned about in comments and mostly handled correctly (pipelined XADD+XACK, cursor-correct XAUTOCLAIM scanning, detached ack contexts, panic-recovering handler dispatch, Is/As-preserving error wrapping). The surviving findings are edge cases and cross-package inconsistencies — an eager default-metrics registration in redisqueue that its redisstream sibling already fixed, a missing invariant check between handlerTimeout and claimMinIdle, and minor metric/comment drift — with nothing rising to HIGH or CRITICAL.

> This scope is unusually disciplined from a security standpoint: resource names go through redis.ValidateName, message/header sizes and counts are bounded before unmarshal (notably the pre-unmarshal 32 KiB headers cap that prevents an OOM), Redis access uses go-redis with proper RESP argument framing so there is no Lua/command injection surface, and errors/PII are consistently routed through the kit's redact wrappers. I found no exploitable injection, auth bypass, crypto misuse, or secret leak; the remaining items are fail-open/unbounded-input edge cases on the dead-letter and delivery-count paths plus one unredacted library-log forwarding gap. Overall a mature, well-commented implementation whose at-least-once and poison-message tradeoffs are mostly explicit in the godoc.

> This is carefully engineered, defensively-written code: the at-least-once/ack/dead-letter paths are heavily commented, detached contexts correctly preserve ack/DLQ writes across shutdown, the removeConsumer PEL-durability guard is thoughtful, and N+1 delivery-count lookups are batched. The main issues are API-consistency gaps across the three sibling packages (metric-registration ordering, nil-registerer handling) rather than functional bugs. The strongest finding is that redisqueue eagerly registers default metrics on the global registry, an anti-pattern its own redisstream sibling documents avoiding.

> This is high-quality, carefully-reasoned code: at-least-once semantics, ack/visibility, poison-message dead-lettering, consumer-group claim/rebalance, and graceful shutdown are all handled deliberately, with detached contexts protecting ACK/DLQ writes against parent cancellation and extensive comments explaining crash windows and idempotency requirements. Concurrency is sound — per-iteration loop vars (Go 1.26), thread-safe Prometheus collectors, mutex-guarded active-queue maps, and correctly-drained claim goroutines with no leaks or races on shared Consumer state. The only real defects I found are a self-defeating lexical sort of stream IDs that quietly reintroduces the N+1 it was meant to remove, and wall-clock-based retention trimming that is skew-sensitive; both leave message-processing correctness intact.

> This is a mature, defensively-written scope: resource names and message envelopes are consistently validated via redis.ValidateName and ValidateMessage, all Redis access goes through parameterized go-redis calls (no injection surface), IDs use crypto-backed UUID v7, metric labels are hashed via OpaqueLabelValue to avoid cardinality/label injection, and logs deliberately omit payloads and headers. The security posture is strong; the issues found are input-validation inconsistencies rather than direct exploits — most notably a producer/consumer header-size cap mismatch that can silently dead-letter valid messages, and an incomplete control-character filter on header values relative to the same code's ID/Type handling.

> This is high-quality, unusually well-documented code: options validate aggressively, handlers are panic-guarded, contexts are correctly detached for ack/dead-letter windows, and the at-least-once/idempotency trade-offs are spelled out in godoc. The main weaknesses are cross-package/API-surface inconsistencies (nil-registerer handling, split producer/consumer payload caps that must be kept in sync manually) and one performance footgun where the batch delivery-count optimization silently degrades to N+1 under same-millisecond bursts due to lexicographic stream-ID sorting. No data-loss or correctness bug rises to CRITICAL/HIGH within this lens.

> This is high-quality, defensively written code: options are validated with panics on programmer error, contexts are deliberately detached for post-handler ACK/dead-letter work, shutdown paths join their goroutines (no obvious leaks or deadlocks), and the retry/dead-letter counting is carefully reasoned with extensive comments. The main residual risks are in the redisstream dead-letter durability path — a non-transactional XADD+XACK pipeline that can drop a message if only the DLQ write fails — and in delivery-count handling where XPENDING errors default to a first-delivery assumption that can defeat poison-message dead-lettering. redisqueue (asynq-backed) and riverqueue are notably solid, with correct idempotency/retry semantics and no concurrency issues found.

## Findings

_All stage-1 findings for this family are fixed or applied as intentional v2 breaks. See V3_BREAKING_PROPOSALS.md (APPLIED) and git history._
